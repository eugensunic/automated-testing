4.) check if duplicate id's exist
5.) check if certain script has loaded
5.) get all SVG's 
6.) get all ID names displayed in a table
10.) check styles tag in html DOM, get amount of styles props being used
13.) check deprecated js functions/syntax
14.) check multiple requests being sent instead of one (happens with observables)
15.) check multiple eventListeners being attached 
16.) check for memory leaks (undisposed objects, eventListeners)
17.) check lazy loading on window.scroll
18.) check if website uses JQuery
19.) check for css !important keyword and find amount of being used
20.) check naming convention used for css classes and id's and see if they match
the ones being used on the page
21.) check number of allowed inputs in input fields and type of inputs, implement
input fields exhaustion
22.) get meta data, title, scripts loading
23.) check which frameworks are used on the website